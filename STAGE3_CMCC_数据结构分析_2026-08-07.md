# Stage 3 CMCC 数据结构分析（2026-08-07）

> **目的**：基于用户提供的数据结构信息，更新 Stage 3 执行方案

---

## 🎯 新信息汇总

### 1. 数据已全部解压 ✅

**关键发现**：tar 文件已全部解压，可以直接访问文件，无需再从 tar 包中提取！

**影响**：
- ✅ I/O 性能大幅提升（直接文件读取 vs tar 解压）
- ✅ 代码已支持：`stage3_gpu.py` 第 70-79 行已实现解压目录优先读取
- ✅ 简化了数据访问逻辑

---

## 📂 数据结构

### 目录层级（4 层）

```
顶层目录（推测）: /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output/
│
├── final_wds-DL3DV-ALL-2K/              # 数据集 1
│   └── wds-DL3DV-ALL-2K/
│       ├── w000/                        # Worker 0
│       │   ├── shard-000000-000000/     # Shard 目录（已解压）
│       │   │   ├── <dataset>_<uuid>.mp4
│       │   │   ├── <dataset>_<uuid>.caption.txt
│       │   │   ├── <dataset>_<uuid>.intrinsics.npy
│       │   │   ├── <dataset>_<uuid>.poses_c2w.npy
│       │   │   ├── <dataset>_<uuid>.meta.json
│       │   │   └── <dataset>_<uuid>.scale.npy
│       │   ├── shard-000000-000000.tar  # 原始 tar（已不需要）
│       │   ├── shard-000000-000000.SUCCESS
│       │   ├── shard-000000-000001/
│       │   ├── shard-000000-000001.tar
│       │   └── ...
│       ├── w001/
│       ├── ...
│       └── w047/                        # Worker 47（共 48 个）
│
├── final_wds-OmniWorld-Game/            # 数据集 2
├── final_wds-RealEstate10K-360p/        # 数据集 3
├── final_wds-SpatialVID-hq/             # 数据集 4
├── final_wds-sekai-game-drone/          # 数据集 5
├── final_wds-sekai-game-walking/        # 数据集 6
├── final_wds-sekai-real-walking-hq/     # 数据集 7
├── final_wds-???/                       # 数据集 8（名称待确认）
│
└── sample_completeness.csv              # 样本完整性标记
```

### 样本文件组成（6 个文件）

每个样本包含：
1. `.mp4` - 视频文件
2. `.caption.txt` - 描述文本
3. `.intrinsics.npy` - 相机内参
4. `.poses_c2w.npy` - 相机位姿
5. `.meta.json` - 元数据
6. `.scale.npy` - 尺度信息

**Stage 3 需要的文件**：
- ✅ `.mp4` - 用于 UniMatch、DOVER
- ✅ `.caption.txt` - 用于 Qwen Caption 改写
- ❌ 其他文件 - Stage 3 不需要

---

## 📊 数据统计

### 样本完整性（sample_completeness.csv）

| 指标 | 数值 |
|------|------|
| 总样本数 | 333,249 |
| 完整样本（TRUE）| 333,163 |
| 不完整样本（FALSE）| 86 |
| 完整率 | 99.97% |

**CSV 格式**：
```csv
sample_id	file_count	complete
sekai-real-walking-hq_JloDXRidyDM_0046950_0048750	5	TRUE
sekai-real-walking-hq__C32UoyEu4U_0090150_0091950	5	TRUE
SpatialVID-hq_3e6204dd-e171-54b0-a3d0-f41fae5abdfd	3	FALSE
```

**注意**：file_count=5 表示缺少 1 个文件（应该有 6 个）

---

## 🗺️ 样本 ID 映射规则

### 样本 ID 格式示例

| 数据集 | 样本 ID 示例 | 格式模式 |
|--------|-------------|---------|
| SpatialVID-hq | `SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18` | `<dataset>_<uuid>` |
| sekai-real-walking-hq | `sekai-real-walking-hq_JloDXRidyDM_0046950_0048750` | `<dataset>_<video_id>_<start>_<end>` |

### 从样本 ID 定位文件路径

**挑战**：样本 ID 不包含 worker 和 shard 信息，需要搜索定位

**方案 1：预建索引**（推荐）
```python
# 一次性扫描，建立 sample_id -> 文件路径 映射
def build_index(data_root):
    index = {}
    for shard_dir in data_root.glob('final_wds-*/wds-*/w*/shard-*'):
        if not shard_dir.is_dir():
            continue
        for mp4_file in shard_dir.glob('*.mp4'):
            sample_id = mp4_file.stem  # 去掉 .mp4 扩展名
            index[sample_id] = {
                'mp4': mp4_file,
                'caption': mp4_file.with_suffix('.caption.txt'),
                'shard_dir': shard_dir
            }
    return index
```

**方案 2：动态搜索**（慢，不推荐）
```python
def find_sample(sample_id, data_root):
    # 在所有 shard 目录中查找
    for mp4_file in data_root.glob(f'final_wds-*/wds-*/w*/shard-*/{sample_id}.mp4'):
        return mp4_file
    return None
```

---

## 🔧 修改要点

### 1. 数据加载模块（新建）

**文件**：`scripts/data_loader_cmcc.py`

**功能**：
```python
class Stage3DataLoaderCMCC:
    def __init__(self, data_root, completeness_csv):
        self.data_root = Path(data_root)
        self.sample_index = self._build_index()
        self.complete_samples = self._load_complete_samples(completeness_csv)
    
    def _build_index(self):
        """扫描所有 shard 目录，建立 sample_id -> 文件路径 映射"""
        pass
    
    def _load_complete_samples(self, csv_path):
        """从 CSV 加载 complete=TRUE 的样本 ID"""
        pass
    
    def get_sample_files(self, sample_id):
        """根据样本 ID 返回文件路径"""
        return {
            'mp4': Path(...),
            'caption': Path(...),
            'shard_dir': Path(...)
        }
```

---

### 2. 冒烟测试脚本（修改）

**文件**：`scripts/run_stage3_single_sample.py`

**修改点**：
- ✅ 已支持直接传入 `--video-path` 和 `--caption-path`
- ⚠️ 需要添加从样本 ID 自动定位文件的功能

**新增参数**：
```bash
python scripts/run_stage3_single_sample.py \
  --sample-id "SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18" \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --auto-locate  # 自动定位文件
```

---

### 3. 完整执行脚本（新建）

**文件**：`scripts/run_stage3_cmcc_full.py`

**架构**：
```
主进程
├── 加载 sample_completeness.csv（333,163 个 TRUE）
├── 预建文件索引（一次性扫描）
├── 将样本均分到 48 个 GPU
└── 启动 48 个 worker 进程
    └── 每个 worker 处理 ~6,940 个样本
```

---

## ❓ 待确认问题

### 必需信息：

1. **数据根目录的绝对路径**
   - 是 `/root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output/` 吗？
   
2. **第 8 个数据集的名称**
   - 你提到有 8 个数据集目录，但只列出了 7 个
   
3. **Stage 1+2 结果位置**
   - 是否存在单独的 Stage 1、Stage 2 结果文件？
   - 还是直接对 CSV 中 complete=TRUE 的样本运行 Stage 3？
   
4. **执行目标**
   - 方案 A：对所有 complete=TRUE 的样本（333,163 个）运行 Stage 3
   - 方案 B：先加载 Stage 1+2 结果，只对通过的样本运行 Stage 3

### 优化选项：

5. **分辨率选择**（默认：256×256）
   - 256×256：最快（28 ms/帧对）
   - 480×480：平衡（~100 ms/帧对）
   
6. **Caption 改写策略**（默认：关键词匹配）
   - 方案 A：关键词匹配预筛选（快）
   - 方案 B：所有样本都过 VLM（准确但慢 2x）

---

## 📝 下一步行动

### 立即执行（等待用户确认）：

1. ✅ 整理数据结构分析（本文档）
2. ⏳ 用户确认 4 个必需问题
3. ⏳ 编写 `data_loader_cmcc.py`（数据加载模块）
4. ⏳ 修改 `run_stage3_single_sample.py`（添加自动定位）
5. ⏳ 编写 `run_stage3_cmcc_full.py`（完整执行脚本）

### 测试流程：

6. ⏳ 单样本测试（1 个样本，验证路径映射）
7. ⏳ 小批量测试（100 个样本，验证性能）
8. ⏳ 全量执行（333,163 个样本，7-8 小时）

---

**文档版本**：v1.0  
**创建日期**：2026-08-07  
**状态**：⏳ 等待用户确认必需信息
