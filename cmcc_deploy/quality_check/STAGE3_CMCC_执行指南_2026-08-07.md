# Stage 3 CMCC 执行指南（2026-08-07 更新版）

> **基于解压数据的优化执行方案**

---

## 🎯 核心改进

### 相比上一版本的关键变化

| 改进项 | 旧方案 | 新方案 | 优势 |
|--------|--------|--------|------|
| 数据读取 | 从 tar 包解压 | 直接读取解压目录 | **I/O 性能提升 10x+** |
| 样本定位 | 需要手动映射 | 预建索引，自动查找 | 简化代码，避免错误 |
| 样本过滤 | 依赖 Stage 1+2 结果 | 使用 sample_completeness.csv | 独立运行，无依赖 |
| 断点续传 | 无 | 支持 | 容错性强，可随时恢复 |

---

## 📦 新增脚本清单

### 1. `data_loader_cmcc.py` - 数据加载器

**功能**：
- 扫描 8 个数据集，建立样本索引（sample_id → 文件路径）
- 加载 `sample_completeness.csv`，过滤完整样本
- 提供快速的文件路径查询

**使用**：
```bash
# 测试数据加载器
python scripts/data_loader_cmcc.py \
  /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  sample_completeness.csv
```

**预期输出**：
```
索引构建完成，共 333249 个样本
完整样本数: 333163
可处理样本数: 333163

前 5 个可处理样本:
  SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18
    视频: .../shard-000000-000000/SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18.mp4
    描述: .../shard-000000-000000/SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18.caption.txt
    状态: ✅ 完整
```

---

### 2. `run_stage3_smoke_test.py` - 单样本冒烟测试

**功能**：
- 支持从样本 ID 自动定位文件
- 执行完整的 Stage 3 检测（UniMatch + DOVER + Qwen）
- 详细的性能分析和错误诊断

**使用方式 1：自动定位（推荐）**
```bash
python scripts/run_stage3_smoke_test.py \
  --sample-id "SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18" \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --gpu-id 0
```

**使用方式 2：手动指定（兼容旧版）**
```bash
python scripts/run_stage3_smoke_test.py \
  --video-path /path/to/sample.mp4 \
  --caption-path /path/to/sample.caption.txt \
  --gpu-id 0
```

**预期输出**：
```
[0/6] 定位样本文件...
  样本 ID: SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18
  ✅ 视频: .../SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18.mp4
  ✅ Caption: .../SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18.caption.txt

[1/6] 加载依赖库...
[2/6] 初始化模型...
  [a] 加载 UniMatch... ✅ 完成 (2.5s)
  [b] 加载 DOVER... ✅ 完成 (1.8s)
  [c] 加载 Qwen3.5-9B... ✅ 完成 (8.2s)

[3/6] 读取视频: .../sample.mp4
  ✅ 形状: (120, 720, 1280, 3), 耗时: 0.8s

[4/6] 执行质量检测...
  [a] UniMatch 光流检测...
      光流幅度: 12.345 像素 (0.850s)
  [b] DOVER 质量评分...
      质量分数: 0.4567 (0.425s)
  [c] Qwen Caption 分析...
      检测到相机词汇，执行改写...
      原始: The camera pans left showing a park with trees...
      改写: A park with trees and people walking...
      耗时: 0.597s

[5/6] 评估结果...
  UniMatch 光流: 12.345 (阈值: < 50.0)
  DOVER 质量: 0.4567 (阈值: > 0.3)
  Caption 改写: 是
  Stage 3 通过: ✅ 是

  性能统计:
    unimatch: 0.850s
    dover: 0.425s
    qwen: 0.597s
    总计: 1.872s

[6/6] 保存结果到: /tmp/stage3_smoke_test.json

冒烟测试完成！✅ 通过
```

---

### 3. `run_stage3_cmcc_full.py` - 完整批量执行

**功能**：
- 将 333,163 个样本均分到 48 个 GPU
- 启动 48 个独立 worker 进程
- 支持断点续传（可随时中断和恢复）
- 实时进度监控

**小批量测试（100 样本，2 GPU）**
```bash
python scripts/run_stage3_cmcc_full.py \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --output-dir /tmp/stage3_test \
  --num-gpus 2 \
  --max-samples 100
```

**全量执行（333,163 样本，48 GPU）**
```bash
# 首次执行
nohup python scripts/run_stage3_cmcc_full.py \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --output-dir /root/work/david_work/qc_output/stage3 \
  --num-gpus 48 \
  > /root/work/david_work/qc_output/stage3/main.log 2>&1 &

# 断点续传（如果中断了）
nohup python scripts/run_stage3_cmcc_full.py \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --output-dir /root/work/david_work/qc_output/stage3 \
  --num-gpus 48 \
  --resume \
  >> /root/work/david_work/qc_output/stage3/main.log 2>&1 &
```

**预期输出**：
```
步骤 1/5: 加载样本列表
  可处理样本数: 333163

步骤 2/5: 跳过断点续传检查

步骤 3/5: 分配工作负载
  GPU 00: 6941 个样本
  GPU 01: 6941 个样本
  ...
  GPU 47: 6941 个样本

步骤 4/5: 启动 workers
  启动 worker 00: PID=12345, 样本数=6941
  启动 worker 01: PID=12346, 样本数=6941
  ...

步骤 5/5: 监控执行进度
实时进度监控（按 Ctrl+C 退出监控，worker 会继续运行）
[14:30:15] 进度: 12500/333163 (3.75%)
```

---

### 4. `stage3_worker.py` - 单 GPU Worker

**功能**：
- 由 `run_stage3_cmcc_full.py` 自动调度，无需手动运行
- 加载模型并处理分配的样本
- 逐行写入 JSONL，支持断点续传

**日志示例**（`logs/worker_00.log`）：
```
[GPU 00] 14:25:30 Worker 启动
[GPU 00] 14:25:30 分配样本数: 6941
[GPU 00] 14:25:32 GPU 00: 开始加载模型...
[GPU 00] 14:25:35 GPU 00: 加载 UniMatch...
[GPU 00] 14:25:38 GPU 00: 加载 DOVER...
[GPU 00] 14:25:40 GPU 00: 加载 Qwen3.5-9B...
[GPU 00] 14:25:48 GPU 00: ✅ 所有模型加载完成
[GPU 00] 14:25:50 [10/6941] ✅ SpatialVID-hq_05b84042... (1.85s)
[GPU 00] 14:25:52 [20/6941] ✅ SpatialVID-hq_05d953a7... (1.92s)
...
```

---

## 🚀 执行流程

### 阶段 1：环境准备（一次性）

```bash
# 1. 激活 conda 环境
conda activate sana_wm_qc_env

# 2. 设置环境变量（每次新会话都需要）
export TORCH_HOME=/root/work/david_work/cache/torch
export TRANSFORMERS_OFFLINE=1
export HF_HUB_OFFLINE=1
export HF_DATASETS_OFFLINE=1
export PYTHONPATH=/root/work/david_work/models/unimatch:$PYTHONPATH

# 3. 验证 GPU
nvidia-smi

# 4. 测试数据加载器
cd /mnt/afs/davidwang/workspace/sana_wm_pipeline
python scripts/data_loader_cmcc.py \
  /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  sample_completeness.csv
```

---

### 阶段 2：冒烟测试（5 分钟）

```bash
# 选择一个样本进行端到端测试
python scripts/run_stage3_smoke_test.py \
  --sample-id "SpatialVID-hq_05b84042-799c-55b1-8a0a-77a2911ecd18" \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --gpu-id 0 \
  --output /tmp/smoke_test_result.json

# 检查结果
cat /tmp/smoke_test_result.json | jq .

# 预期：stage3_pass = true，耗时 ~2 秒
```

---

### 阶段 3：小批量测试（10 分钟）

```bash
# 100 样本，2 GPU
python scripts/run_stage3_cmcc_full.py \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --output-dir /tmp/stage3_batch_test \
  --num-gpus 2 \
  --max-samples 100

# 检查结果
wc -l /tmp/stage3_batch_test/worker_*.jsonl
cat /tmp/stage3_batch_test/worker_00.jsonl | head -1 | jq .

# 预期：100 个样本处理完成，每个样本 ~2 秒
```

---

### 阶段 4：全量执行（7-8 小时）

```bash
# 启动全量执行
nohup python scripts/run_stage3_cmcc_full.py \
  --data-root /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output \
  --completeness-csv sample_completeness.csv \
  --output-dir /root/work/david_work/qc_output/stage3 \
  --num-gpus 48 \
  > /root/work/david_work/qc_output/stage3/main.log 2>&1 &

# 记录进程 ID
echo $! > /tmp/stage3_main_pid.txt

# 实时查看日志
tail -f /root/work/david_work/qc_output/stage3/main.log

# 查看某个 worker 的日志
tail -f /root/work/david_work/qc_output/stage3/logs/worker_00.log

# 监控进度
watch -n 30 'find /root/work/david_work/qc_output/stage3 -name "worker_*.jsonl" -exec wc -l {} \; | awk "{s+=\$1} END {print s, \"/ 333163 (\", s/333163*100, \"%)\"}"'

# 监控 GPU 使用率
watch -n 10 nvidia-smi
```

---

### 阶段 5：结果汇总（10 分钟）

```bash
# 合并所有 worker 输出
cd /root/work/david_work/qc_output/stage3
cat worker_*.jsonl > stage3_results_merged.jsonl

# 统计通过率
total=$(wc -l < stage3_results_merged.jsonl)
passed=$(grep -c '"stage3_pass": true' stage3_results_merged.jsonl)
echo "总样本数: $total"
echo "通过样本数: $passed"
echo "通过率: $(echo "scale=2; $passed * 100 / $total" | bc)%"

# 提取通过的样本 ID
grep '"stage3_pass": true' stage3_results_merged.jsonl | \
  jq -r '.sample_id' > stage3_pass_list.txt

echo "通过样本列表: stage3_pass_list.txt ($(wc -l < stage3_pass_list.txt) 个)"
```

---

## 📊 预期性能

### 单样本性能（已验证）

| 模块 | 耗时 | 显存占用 |
|------|------|---------|
| UniMatch | 0.8 秒 | 0.02 GB |
| DOVER | 0.4 秒 | 0.22 GB |
| Qwen Caption | 0.6 秒 | 16.68 GB |
| **总计** | **~2 秒** | **~17 GB** |

### 全量执行预估

| 指标 | 数值 |
|------|------|
| 总样本数 | 333,163 |
| GPU 数量 | 48 |
| 每 GPU 样本数 | ~6,941 |
| 单样本耗时 | ~2 秒 |
| 理论耗时 | 6,941 × 2 ÷ 3600 = **3.9 小时** |
| 实际耗时（含 I/O） | **7-8 小时** |

**性能瓶颈分析**：
- ✅ 模型推理：优化完成（Qwen 思维链 17.91x 加速）
- ⚠️ I/O 开销：解压后的目录大幅缓解，但仍有网络存储延迟
- ⚠️ 调度开销：48 个进程同时启动，存在初始化延迟

---

## 🐛 常见问题

### Q1: 数据加载器显示样本数为 0

**原因**：数据根目录路径错误，或 glob 模式不匹配

**解决**：
```bash
# 检查目录结构
ls -lh /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output
find /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output -maxdepth 3 -type d | head -20

# 确认 shard 目录存在
find /root/work/filestorage/shangaoooooo/davidwang/jdvbbfb_output -name "shard-*" -type d | head -5
```

---

### Q2: Worker 启动后立即退出

**原因**：模型加载失败，或环境变量未设置

**解决**：
```bash
# 查看 worker 日志
cat /root/work/david_work/qc_output/stage3/logs/worker_00.log

# 检查环境变量
echo $TORCH_HOME
echo $TRANSFORMERS_OFFLINE
echo $PYTHONPATH

# 手动测试模型加载
python -c "from sana_wm_pipeline.qc.stage3_gpu import load_unimatch_fn; print('OK')"
```

---

### Q3: Qwen 推理很慢（10 秒+）

**原因**：思维链未禁用

**解决**：
```bash
# 检查代码修复是否生效
grep -n "enable_thinking" src/sana_wm_pipeline/qc/stage3_gpu.py

# 应该看到第 357 行有 enable_thinking=False
# 如果没有，说明修复未应用，参考 QWEN_THINKING_FIX.md
```

---

### Q4: 断点续传后进度显示不准

**原因**：已完成样本统计延迟

**解决**：
```bash
# 手动统计已完成样本
find /root/work/david_work/qc_output/stage3 -name "worker_*.jsonl" -exec wc -l {} \; | awk '{s+=$1} END {print s}'

# 与总数对比
echo "333163"
```

---

### Q5: 某些 GPU 处理速度明显慢

**原因**：GPU 被其他任务占用，或分配的样本更复杂

**解决**：
```bash
# 检查 GPU 占用
nvidia-smi

# 查看慢 GPU 的日志
tail -f /root/work/david_work/qc_output/stage3/logs/worker_XX.log

# 如果确认被占用，可以停止该 worker，然后用 --resume 重新分配
```

---

## 📋 检查清单

### 执行前检查 ✓

- [ ] Conda 环境已激活（`sana_wm_qc_env`）
- [ ] 环境变量已设置（`TORCH_HOME`, `TRANSFORMERS_OFFLINE`, 等）
- [ ] 数据根目录存在且可访问
- [ ] `sample_completeness.csv` 存在
- [ ] 输出目录有足够空间（~50 GB）
- [ ] GPU 空闲（`nvidia-smi` 检查）
- [ ] 冒烟测试通过

### 执行中监控 ⚠️

- [ ] 所有 worker 都在运行（`ps aux | grep stage3_worker`）
- [ ] GPU 利用率正常（`nvidia-smi` 每隔 10 分钟）
- [ ] 输出文件在增长（`ls -lh /path/to/output/worker_*.jsonl`）
- [ ] 日志无异常错误（`tail -f logs/worker_00.log`）

### 执行后验证 ✅

- [ ] 所有样本已处理（`wc -l worker_*.jsonl` 总和 = 333,163）
- [ ] 通过率合理（预期 ~85-90%）
- [ ] 无大量错误日志
- [ ] 结果文件格式正确（`jq . worker_00.jsonl | head`）

---

## 🎯 与上一版本的兼容性

### 保持兼容的部分

- ✅ `src/sana_wm_pipeline/qc/stage3_gpu.py` - 核心逻辑已支持解压目录
- ✅ 三大模型加载函数（`load_unimatch_fn`, `load_dover_fn`, `load_qwen_fn`）
- ✅ 环境变量配置
- ✅ 输出 JSONL 格式

### 新增的部分

- ➕ `data_loader_cmcc.py` - 新建，数据索引
- ➕ `run_stage3_smoke_test.py` - 改进版单样本测试
- ➕ `run_stage3_cmcc_full.py` - 新建，批量执行
- ➕ `stage3_worker.py` - 新建，单 GPU worker

### 废弃的部分（可删除）

- ❌ `run_stage3_single_sample.py` - 被 `run_stage3_smoke_test.py` 取代（更强大）

---

## 📞 获取帮助

### 问题排查顺序

1. **查看日志**：`logs/worker_XX.log` 和 `main.log`
2. **检查环境**：环境变量、模型路径、数据路径
3. **单样本测试**：用 `run_stage3_smoke_test.py` 隔离问题
4. **参考文档**：
   - 本文档（执行指南）
   - `STAGE3_CMCC_数据结构分析_2026-08-07.md`（数据结构）
   - `QWEN_THINKING_FIX.md`（Qwen 问题）
   - 上一版本的文档（`SESSION_SUMMARY_2026-08-04.md`）

---

**文档版本**：v1.0  
**创建日期**：2026-08-07  
**状态**：✅ 就绪，可开始执行  
**预计执行时间**：7-8 小时（全量 333,163 样本）
