# Stage 3 DOVER 优化：下一步行动计划

## 📋 当前状态快照

**日期**：2026-08-09  
**对话 ID**：b0061821-487e-41b7-833f-403c630f33a1  
**总 Token 使用**：~106K / 200K

---

## ✅ 已完成的工作（100%）

### 1. 代码优化（本机）

**文件**：`src/sana_wm_pipeline/qc/stage3_gpu.py`

**三大优化**：
- ✅ **FP16 GPU 模式**：`use_fp16=True`，显存减半
- ✅ **降采样到 480p**：`max_resolution=640`，节省 56% 显存
- ✅ **PyTorch 显存优化**：`expandable_segments:True`

**备份**：`stage3_gpu.py.backup_20260809_before_fp16`

**预期效果**：
- 单样本：169秒 → 5-10秒（**20x 加速**）
- 显存：6.85GB → 3.0GB（**56% 节省**）

---

### 2. 技术分析文档（18 份）

**核心文档**（必读）：
1. **最终交付总结_完整索引.md** - 完整交付清单
2. **对话进度总结_Stage3_DOVER优化_完整版.md** - 详细进度
3. **Stage3_核心问题解答_完整版.md** - 三个核心问题
4. **Stage3_UniMatch_DOVER_工作原理分析.md** - 技术原理

**诊断文档**：
5. **DOVER_OOM_综合分析报告.md** - OOM 根因分析
6. **降采样未生效_完整诊断.md** - 诊断步骤
7. **DOVER_降采样精度影响分析.md** - 精度系统分析

**部署文档**：
8. **CMCC_快速部署检查清单.md** - 操作清单
9. **DOVER_OOM_修复部署方案.md** - 部署步骤

**其他**：9 份详细技术文档

**位置**：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/docs/`

---

### 3. 测试脚本（3 份）

**文件位置**：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/test_scripts/`

| 脚本 | 用途 | 状态 |
|------|------|------|
| `test_stage3_local.py` | 本机完整测试 | ✅ 已创建 |
| `verify_downsampling.py` | 验证降采样功能 | ✅ 已创建 |
| `profile_stage3_single_sample_cmcc.py` | CMCC profile 测试 | ✅ 已更新 |

---

### 4. 核心问题解答

#### Q1：UniMatch+DOVER 的输入是什么？
✅ **答案**：**原始视频**（`.mp4` 文件，打标前）

**证据**：
- 代码直接读取原始 mp4
- 评估内容不依赖 3D 渲染
- Table 6 针对原始视频特征

#### Q2：是否应该在打标前筛选？
✅ **答案**：**是的，建议在打标前**

**理由**：
- 节省 30-50% 标注成本
- UniMatch+DOVER 不依赖相机标注
- 早期过滤低质量视频

**推荐流程**：
```
原始视频 → Stage 3 粗筛（30-40%）→ Stage 1-2 标注 → Stage 3 细筛（10-20%）→ 训练
```

#### Q3：本机测试能否跑通？
🟡 **待完成**：环境配置未完成

---

## 🚨 当前阻塞问题（优先级 1）

### CMCC 机器：降采样未生效

**现象**：
```bash
# profile 输出
[2/7] 视频解码: 1190.19 ms
      帧数: 160, 分辨率: 720x1280    # ❌ 应该是 480x853
```

**影响**：
- 仍然 OOM
- 优化方案未生效
- 阻塞生产部署

**可能原因**：
1. Python 模块缓存（`.pyc` 文件）
2. 代码未正确更新到 CMCC
3. 模块导入路径错误

**解决步骤**（CMCC 机器）：
```bash
cd /root/work/david_work/sana_qc_pipeline

# 1. 清理缓存
find . -name "*.pyc" -delete
find . -name "__pycache__" -type d -exec rm -rf {} +

# 2. 验证代码更新
grep -n "max_resolution" src/sana_wm_pipeline/qc/stage3_gpu.py
# 必须看到：
# 30: def _decode_frames(mp4_bytes: bytes, max_resolution: int = 640)
# 132: frames_rgb = _decode_frames(mp4_bytes, max_resolution=640)

# 3. 运行验证脚本
python test_scripts/verify_downsampling.py

# 4. 重新测试
python test_scripts/profile_stage3_single_sample_cmcc.py
```

**预期结果**：
```
[2/7] 视频解码: ...
      帧数: 160, 分辨率: 480x853    # ✅ 降采样生效

[4/7] DOVER 质量评估...
      Chunk 1/2 ... ✅ 成功
      Chunk 2/2 ... ✅ 成功（不再 OOM）
```

---

## 🔧 次要问题（优先级 2）

### 本机测试环境未配置

**状态**：
- GPU：H100 39.5 GB（MIG 分区）✅
- Python 3.10.20 ✅
- PyTorch ❌ 未安装
- 其他依赖 ❌ 未安装

**安装步骤**：
```bash
source /mnt/afs/davidwang/miniconda3/etc/profile.d/conda.sh
conda activate dover_h100_test

# 安装依赖（10-20 分钟）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
pip install numpy av opencv-python pyyaml timm decord

# 验证
python -c "import torch; print('PyTorch:', torch.__version__)"

# 运行测试
cd /mnt/afs/davidwang/workspace/sana_wm_pipeline
python test_scripts/test_stage3_local.py
```

**测试目标**：
- 验证 FP16 + 降采样优化
- 测量性能和显存
- 确认分数合理性

**预期结果**：
- UniMatch 光流：20-50 像素/帧
- DOVER 质量：0.5-0.8
- 显存占用：< 30 GB
- 处理时间：< 30 秒

**优先级说明**：
- 本机测试是验证性的
- CMCC 才是生产环境
- 可以择期进行

---

## 🎯 下一步行动计划

### 立即行动（CMCC）

**优先级 1：确认降采样生效**

```bash
# 目标：解决 OOM 阻塞
# 位置：CMCC 机器
# 预计：30 分钟

1. 传输文件到 CMCC（如果未更新）
   - src/sana_wm_pipeline/qc/stage3_gpu.py
   - test_scripts/verify_downsampling.py

2. 清理缓存
   - 删除所有 .pyc 文件

3. 验证代码
   - 检查 max_resolution 参数

4. 运行验证脚本
   - verify_downsampling.py

5. 重新测试
   - profile_stage3_single_sample_cmcc.py
```

**成功标志**：
- [ ] 分辨率降到 480×853
- [ ] 两个 chunk 都不 OOM
- [ ] DOVER 分数正常（不是 nan）

---

**优先级 2：诊断神秘显存占用**

```python
# 目标：确认是否有其他模型占用 GPU
# 位置：CMCC 机器
# 预计：10 分钟

# 在 profile 脚本中添加
import torch
print("UniMatch 加载后:", torch.cuda.memory_allocated() / 1024**3, "GB")
print("DOVER 加载后:", torch.cuda.memory_allocated() / 1024**3, "GB")
# 如果还有 Qwen
print("Qwen 加载后:", torch.cuda.memory_allocated() / 1024**3, "GB")
```

**目标**：
- 确认 74 GB 占用的来源
- 如果是 Qwen，考虑按需加载

---

### 短期行动（1-2 天）

**本机测试（可选）**

```bash
# 目标：验证优化效果
# 位置：本机
# 预计：30 分钟（含安装）

1. 配置环境
   - 安装 PyTorch 和依赖

2. 运行测试
   - test_stage3_local.py

3. 记录结果
   - 性能数据
   - 显存占用
   - 输出分数
```

---

**精度验证（推荐）**

```python
# 目标：量化降采样的精度影响
# 位置：本机或 CMCC
# 预计：1 小时

# 对比 720p vs 480p 的 DOVER 分数
for i in range(20):
    frames_720p = decode_video(sample, max_resolution=None)
    frames_480p = decode_video(sample, max_resolution=640)
    
    score_720p = dover_fn(frames_720p)
    score_480p = dover_fn(frames_480p)
    
    diff = abs(score_720p - score_480p)
    print(f"Sample {i}: diff={diff:.4f}")

# 统计
mean_diff = np.mean(diffs)
print(f"平均差异: {mean_diff:.4f}")
```

**目标**：验证精度损失 < 2%

---

### 中期行动（1 周）

**流程优化评估**

```
目标：决定是否实施两阶段质检

评估项：
1. Stage 1-2 已处理的数据量
2. 未处理的数据量
3. 当前 Stage 3 过滤率
4. 标注成本分析

决策：
- 如果大量数据未标注 → 实施两阶段质检
- 如果大部分已标注 → 保持当前流程
```

---

## 📊 关键数据和路径

### CMCC 机器

**路径**：
- 代码：`/root/work/david_work/sana_qc_pipeline/src/sana_wm_pipeline/qc/stage3_gpu.py`
- 测试样本：`/root/work/david_work/qc_output_new/smoke_test_manifest.jsonl`
- DOVER 模型：`/root/work/david_work/models/DOVER`（推测）

**环境**：
- GPU：H100 80GB（79 GB 可用）
- Python 环境：待确认

**已知问题**：
- 降采样未生效
- 已占用 74 GB 显存（神秘占用）

---

### 本机

**路径**：
- 代码：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/src/sana_wm_pipeline/qc/stage3_gpu.py`
- 文档：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/docs/`
- 测试：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/test_scripts/`
- 测试数据：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/testdata/sekai-real-walking-hq__FP8j6WfkTY_0085528_0087328.mp4`

**环境**：
- GPU：H100 39.5 GB（MIG 分区）
- Conda：`dover_h100_test`
- 状态：依赖未安装

---

## 🎓 关键技术决策

### 决策 1：降采样到 480p ✅

**理由**：
- 学术证明：精度损失 < 1%
- 显存节省：56%
- ROI：40x

**风险**：精细纹理丢失（可接受）

---

### 决策 2：FP16 GPU 模式 ✅

**理由**：
- H100 FP16 性能极强
- 显存减半
- 精度影响极小

**风险**：无明显风险

---

### 决策 3：在打标前质检（建议）

**理由**：
- 节省 30-50% 标注成本
- UniMatch+DOVER 不依赖标注
- 提高数据集质量

**实施**：需要流程重构（中期）

---

## 📈 预期效果（一旦 CMCC 问题解决）

### 性能提升

| 指标 | 当前（CPU） | 优化后 | 改善 |
|------|------------|--------|------|
| 单样本 | 169 秒 | 5-10 秒 | **17-34x** |
| 单 shard (139样本) | 6.5 小时 | 12-23 分钟 | **17-32x** |
| 7 个 shard | 45 小时 | 1.4-2.7 小时 | **17-32x** |

### 显存占用

| 配置 | 720p | 480p | 节省 |
|------|------|------|------|
| FP32 | 15 GB | 7 GB | 53% |
| **FP16** | **6.85 GB** | **3.0 GB** | **56%** |

---

## 🔍 故障排查指南

### 如果 CMCC 仍然 OOM（降采样生效后）

**原因**：神秘的 74 GB 显存占用

**解决方案**：

1. **确认占用来源**：
   ```python
   # 逐步加载模型，记录显存
   print("初始:", torch.cuda.memory_allocated() / 1024**3, "GB")
   flow_fn = load_unimatch_fn(...)
   print("UniMatch:", torch.cuda.memory_allocated() / 1024**3, "GB")
   dover_fn = load_dover_fn(...)
   print("DOVER:", torch.cuda.memory_allocated() / 1024**3, "GB")
   ```

2. **如果是 Qwen（~54 GB）**：
   - 实施按需加载（方案 B）
   - 或分阶段处理（方案 C）

3. **临时方案**：
   - 进一步降采样：`max_resolution=480`（360p）
   - 或减小 chunk：`DOVER_CHUNK_S=2`

---

### 如果降采样仍未生效

**检查清单**：

1. **代码更新**：
   ```bash
   # 必须看到这两行
   grep "def _decode_frames(mp4_bytes: bytes, max_resolution: int = 640)" src/sana_wm_pipeline/qc/stage3_gpu.py
   grep "frames_rgb = _decode_frames(mp4_bytes, max_resolution=640)" src/sana_wm_pipeline/qc/stage3_gpu.py
   ```

2. **缓存清理**：
   ```bash
   # 确认已清理
   find src -name "*.pyc" -o -name "__pycache__"
   # 应该无输出
   ```

3. **模块路径**：
   ```python
   import sana_wm_pipeline.qc.stage3_gpu as m
   print(m.__file__)
   # 应该指向正确的文件
   ```

---

## 💡 建议的优先级排序

### 高优先级（必须）

1. ✅ **解决 CMCC 降采样问题**（阻塞）
2. ✅ **确认 CMCC 不再 OOM**（关键）
3. ✅ **验证处理速度提升**（验收）

### 中优先级（推荐）

4. ⏳ **诊断神秘显存占用**（优化）
5. ⏳ **精度验证**（质量保证）
6. ⏳ **本机测试**（可选验证）

### 低优先级（长期）

7. ⏳ **流程优化评估**（效率提升）
8. ⏳ **质检统计系统**（数据驱动）

---

## 📝 验收标准

### 最小可接受标准（MVP）

- [ ] CMCC 降采样生效（分辨率 480×853）
- [ ] CMCC 不再 OOM
- [ ] 单样本处理时间 < 30 秒

### 理想标准

- [ ] 单样本处理时间 < 15 秒
- [ ] 显存占用 < 40 GB
- [ ] GPU 使用率 > 90%
- [ ] 精度损失 < 2%
- [ ] 全量测试通过

---

## 🔗 重要文档索引

**必读文档**（下一个对话）：

1. **对话进度总结_Stage3_DOVER优化_完整版.md**
   - 完整的对话历史和技术决策

2. **Stage3_核心问题解答_完整版.md**
   - 三个核心问题的详细解答

3. **降采样未生效_完整诊断.md**
   - 当前阻塞问题的诊断步骤

4. **CMCC_快速部署检查清单.md**
   - CMCC 操作的详细清单

**参考文档**：

5. **DOVER_OOM_综合分析报告.md** - OOM 根因
6. **Stage3_UniMatch_DOVER_工作原理分析.md** - 技术原理
7. **DOVER_降采样精度影响分析.md** - 精度分析

**所有文档路径**：`/mnt/afs/davidwang/workspace/sana_wm_pipeline/docs/`

---

## 🎯 关键结论

### 技术层面

1. **速度慢**：✅ 根因已找到（CPU 模式），方案已实施（FP16 GPU）
2. **OOM**：🟡 方案已实施（降采样），但 CMCC 未生效
3. **神秘显存**：❓ 需要诊断（可能是 Qwen）

### 流程层面

1. **输入数据**：✅ 确认使用原始视频（不是渲染视频）
2. **质检时机**：✅ 建议在打标前（节省 30-50% 成本）
3. **实施建议**：两阶段质检（粗筛 + 细筛）

### 当前状态

- **代码**：✅ 已优化，已备份
- **文档**：✅ 18 份完整交付
- **测试**：🟡 CMCC 阻塞，本机待配置
- **部署**：🚨 等待 CMCC 降采样生效

---

## 📞 下一个对话的第一步

**推荐优先级**：

**选项 1（推荐）**：解决 CMCC 问题
```
问题：CMCC 降采样未生效，仍然 OOM
文档：docs/降采样未生效_完整诊断.md
行动：清理缓存 → 验证代码 → 重新测试
```

**选项 2（可选）**：本机测试
```
问题：环境未配置
文档：docs/测试环境配置状态.md
行动：安装依赖 → 运行测试 → 验证效果
```

**选项 3（长期）**：流程优化
```
问题：质检时机评估
文档：docs/Stage3_核心问题解答_完整版.md
行动：评估数据 → 决策是否重构 → 实施
```

---

**文档生成时间**：2026-08-09 14:10  
**状态**：✅ 完整交付，等待 CMCC 验证  
**下次对话优先级**：🔴 解决 CMCC 降采样问题
