# Stage 3 测试环境配置状态

## 📊 当前状态

**检查时间**：2026-08-09 12:45

### 环境配置状态：❌ 未完成

**dover_h100_test 环境**：
- Python：3.10.20 ✅ 已安装
- PyTorch：❌ 未安装
- NumPy：❌ 未安装
- av（视频解码）：❌ 未安装
- opencv-python：❌ 未安装

**后台安装任务**：
- 任务 ID：`bv5defjaa`
- 状态：运行中或已失败（无输出）
- 命令：`pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121`

---

## 🔧 手动安装步骤

如果需要立即测试，可以手动安装依赖：

```bash
# 激活环境
source /mnt/afs/davidwang/miniconda3/etc/profile.d/conda.sh
conda activate dover_h100_test

# 安装 PyTorch（CUDA 12.1）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# 安装其他依赖
pip install numpy av opencv-python decord pyyaml timm

# 验证安装
python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA:', torch.cuda.is_available())"
```

**预计安装时间**：5-15 分钟（取决于网络速度）

---

## 🧪 测试准备情况

### ✅ 已就绪

- [x] 测试脚本：`test_scripts/test_stage3_local.py`
- [x] 测试数据：`testdata/sekai-real-walking-hq__FP8j6WfkTY_0085528_0087328.mp4`
- [x] 模型路径：`models/DOVER/` 和 `models/unimatch/`
- [x] 优化代码：`src/sana_wm_pipeline/qc/stage3_gpu.py`

### ❌ 待完成

- [ ] PyTorch 安装
- [ ] 依赖包安装
- [ ] 环境验证

---

## ⏱️ 预计时间表

### 场景 A：自动安装成功

**条件**：后台任务成功完成
- 检查任务输出：`cat /tmp/claude-0/-mnt-afs-davidwang-workspace/*/tasks/bv5defjaa.output`
- 如果成功：立即可以测试
- **预计**：已就绪 或 失败

### 场景 B：手动安装

**步骤**：
1. 手动运行上述安装命令（5-15 分钟）
2. 验证环境（1 分钟）
3. 运行测试（2-5 分钟）

**总计**：约 10-20 分钟

---

## 🚀 启动测试的命令

一旦环境配置完成，运行：

```bash
cd /mnt/afs/davidwang/workspace/sana_wm_pipeline
source /mnt/afs/davidwang/miniconda3/etc/profile.d/conda.sh
conda activate dover_h100_test

# 运行测试
python test_scripts/test_stage3_local.py
```

**预期测试时间**：2-5 分钟（960 帧视频）

---

## 📋 测试内容

测试将验证：
1. ✅ 视频解码（原始 vs 降采样）
2. ✅ UniMatch 模型加载和推理
3. ✅ DOVER 模型加载和推理（FP16 GPU）
4. ✅ 显存占用监控
5. ✅ 处理速度测量
6. ✅ 输出分数与 Table 6 阈值对比

---

## 🎯 预期测试结果

基于理论分析，预期：

| 指标 | 预期值 | 说明 |
|------|--------|------|
| **视频解码** | 降采样到 480×853 | 从 720×1280 降采样 |
| **UniMatch 光流** | 20-50 像素/帧 | 步行视角，中等运动 |
| **DOVER 质量** | 0.5-0.8 | 高清视频，质量较好 |
| **显存占用** | < 30 GB | 本机 GPU 有 39.5 GB |
| **处理时间** | < 30 秒 | 960 帧，FP16 GPU 模式 |
| **Table 6 判定** | ✅ 通过 | 符合 sekai-real-walking 阈值 |

---

## ⚠️ 已知限制

### 本机 GPU

- **型号**：H100 80GB HBM3 MIG 3g.40gb
- **可用显存**：39.5 GB（非完整 80GB）
- **限制**：MIG 分区模式

**影响**：
- 可以测试基本功能
- 但与 CMCC 的 79 GB 环境不同
- 如果本机成功，CMCC 应该也能成功

### DOVER 模型依赖

可能需要额外安装：
```bash
pip install decord  # 视频解码（DOVER 内部可能需要）
pip install timm    # Vision Transformer（DOVER 依赖）
```

---

## 📞 下一步行动

### 选项 1：等待自动安装（推荐）

检查后台任务是否完成：
```bash
cat /tmp/claude-0/-mnt-afs-davidwang-workspace/*/tasks/bv5defjaa.output
```

### 选项 2：手动安装（快速）

如果需要立即测试，执行手动安装步骤。

### 选项 3：使用其他环境

如果 `dover_h100_test` 配置困难，可以考虑：
- 使用 `abot-physworld` 环境（需要安装 DOVER 依赖）
- 创建新的虚拟环境

---

## 📝 决策建议

**如果时间不紧急**：
- 等待后台安装完成
- 或择期手动配置

**如果需要立即验证**：
- 手动安装依赖（10-20 分钟）
- 运行测试
- 验证优化效果

**如果本机测试不重要**：
- 专注于 CMCC 部署
- 确认降采样生效
- 解决 OOM 问题

---

**状态更新时间**：2026-08-09 12:45  
**下次检查建议**：15 分钟后或手动安装后
