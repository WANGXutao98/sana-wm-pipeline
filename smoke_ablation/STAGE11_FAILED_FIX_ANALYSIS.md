# 阶段11调查最新进展：修复未生效分析

**日期**: 2026-08-14  
**状态**: ❌ 第一帧归一化修复未解决问题  
**当前轨迹偏差**: 仍然是 2.24-10.7x

---

## 修复尝试1：移除第一帧归一化 ❌

### 修改内容
删除了 `mode_default.py:228-231` 的第一帧归一化逻辑

### 结果
| 样本 | 修复前 | 修复后 | VIPE参考 | 状态 |
|------|--------|--------|----------|------|
| 样本1 | 2.46x | 2.24x | 0.0247m | ❌ 仍偏大 |
| 样本2 | 10.73x | 10.70x | 0.2331m | ❌ 几乎无变化 |
| 样本3 | - | 4.03x | 2.6344m | ❌ 新发现偏差 |

**结论**: 第一帧归一化不是主要问题（或不是唯一问题）

---

## 已验证的事实

### ✅ 确认正确的部分

1. **融合深度被正确使用**
   - Pi3xMogeModel日志显示: `depth range: 2.52-34.05m, mean: 12.02m`
   - 与预计算数据完全一致
   - VIPE确实读取并使用了我们的融合深度

2. **Scale传递正常**
   - 样本1: 1.399-1.409
   - 样本2: 0.694-0.756
   - 样本3: 2.322-2.369
   - CoV都 < 0.03，远低于阈值

3. **坐标系一致**
   - 第一帧位置差异 < 0.5mm
   - 第一帧旋转差异 < 0.001
   - 与SpatialVID参考使用相同坐标系

4. **深度数值合理**
   - 范围: 0.7-109m（米制）
   - 符合室内外真实场景

5. **插值不是问题**
   - SpatialVID参考: sparse (13) → dense (50)，轨迹长度相同
   - 线性插值不会改变直线轨迹长度

### ❌ 发现的差异

1. **轨迹长度本身不同**
   ```
   样本1:
     我们的输出: 0.0553m (32 keyframes)
     参考sparse: 0.0247m (13 keyframes)
     比例: 2.24x
   ```
   
2. **Keyframe密度不同**
   ```
   参考: 13 keyframes (间隔4帧, vipe_frame_skip=4)
   我们: 32 keyframes (kf_gap_sec=2.0, 视频16fps)
   ```
   但keyframe密度差异不应该影响轨迹scale，只影响密度。

3. **内参略有差异**
   ```
   参考: fx=681.7 (raw sparse) → fx=705.5 (interpolated)
   我们: fx=692.8 (始终不变)
   ```

---

## 当前假设

### 假设1: VIPE BA调整了深度scale ⭐

**依据**:
- 我们给VIPE的深度是正确的
- VIPE使用了这个深度
- 但输出的轨迹scale仍然偏大
- → VIPE的BA可能重新优化了全局scale

**验证方法**:
1. 查看VIPE源码的BA优化变量
2. 检查是否有全局scale参数
3. 添加日志记录BA前后的scale变化

### 假设2: VIPE配置差异

**可能的差异点**:
- `kf_gap_sec: 2.0` vs SpatialVID的配置
- BA参数（迭代次数、收敛阈值）
- 深度权重设置

**验证方法**:
1. 对比SpatialVID生成数据时的VIPE配置
2. 检查是否有未记录的配置差异

### 假设3: SpatialVID参考数据的生成方法不同

**可能性**:
- SpatialVID可能使用了不同版本的VIPE
- 或使用了不同的深度融合参数
- 或有额外的后处理步骤

**验证方法**:
1. 查看SpatialVID论文/代码的数据生成细节
2. 对比`vipe_source: nv-tlabs/vipe`的具体版本

### 假设4: 我们的融合深度计算有系统性偏差

**可能性**:
- MoGe深度的scale标定有误
- EMA平滑参数不匹配
- 或者还有其他未发现的bug

**验证方法**:
1. 逐步对比官方融合深度实现的每个细节
2. 检查MoGe模型输出的scale是否正确
3. 对比Pi3X的scale-ambiguous深度

---

## 下一步行动

### 优先级1: 深入分析VIPE BA过程

```bash
# 添加更多日志到VIPE源码
# 查看 vipe/slam/components/backend.py 和 buffer.py
# 记录BA前后的:
# - disps (逆深度)
# - poses (位姿)
# - 全局scale参数（如果有）
```

### 优先级2: 对比VIPE配置

```bash
# 查找SpatialVID数据集的VIPE配置
# 对比:
# - kf_gap_sec / frame_skip
# - BA迭代次数
# - 深度权重
```

### 优先级3: 运行最小验证

创建一个最简单的测试case:
1. 人工构造一个已知轨迹长度的场景
2. 生成对应的深度图
3. 运行VIPE，看输出轨迹是否匹配预期

---

## 已排除的假设

| 假设 | 验证方法 | 结果 |
|------|---------|------|
| 第一帧归一化 | 删除代码重测 | ❌ 无显著改善 |
| 融合深度未被使用 | 检查日志 | ❌ 已确认使用 |
| 深度单位错误 | 实测数值 | ❌ 米制正确 |
| Scale未传递 | 检查artifact | ❌ 已正确传递 |
| 坐标系不一致 | 对比第一帧 | ❌ 坐标系一致 |
| 插值拉长轨迹 | 数学验证 | ❌ 不影响直线 |

---

## 关键观察

1. **修复前后变化很小**: 2.46x → 2.24x (样本1), 10.73x → 10.70x (样本2)
   - 说明第一帧归一化确实有小影响（~9%）
   - 但不是主要问题

2. **不同样本偏差不一致**: 2.24x, 10.70x, 4.03x
   - 说明不是固定的scale factor
   - 可能与场景特征相关（深度范围、轨迹复杂度等）

3. **深度数据无误**:
   - 预计算正确
   - VIPE读取正确
   - 数值合理

4. **问题在VIPE内部或配置**:
   - 排除了输入数据问题
   - 排除了事后处理问题
   - 剩下的只有VIPE运行过程

---

## 建议

### 短期（调查方向）

1. **添加VIPE内部日志**: 记录BA优化过程的scale变化
2. **对比VIPE配置**: 查找SpatialVID使用的确切配置
3. **检查VIPE版本**: 确认我们使用的VIPE版本与SpatialVID一致

### 长期（如果仍无法定位）

1. **联系SpatialVID作者**: 询问数据生成的详细配置
2. **运行官方baseline**: 使用SpatialVID提供的脚本重新生成数据对比
3. **接受偏差**: 如果影响不大，可能需要接受这个系统性偏差

---

## 文件记录

1. `FINAL_INVESTIGATION_SUMMARY.md` - 之前的总结（第一帧归一化分析）
2. `STAGE11_FAILED_FIX_ANALYSIS.md` - 本文档（修复未生效分析）
3. `spatialvid_smoke_test2.log` - 最新测试日志

---

**当前状态**: 需要更深入的VIPE内部调查  
**预计难度**: HIGH（需要理解VIPE BA的细节）  
**建议**: 暂停修复，先完成其他阶段，稍后回到这个问题
